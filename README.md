# postman-app
A Postman-like web application built with Vue 3 and TypeScript, allowing developers to create, organize, and send HTTP requests to APIs directly in the browser. It replicates core API testing features such as request collections, response previews, and endpoint debugging without requiring external tools.
